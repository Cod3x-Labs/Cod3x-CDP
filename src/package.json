{
  "name": "pluc-stablecoin",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "hardhat clean",
    "lint": "solhint list-rules && solhint contracts/**/*.sol test/**/*.sol",
    "format": "prettier --write contracts/**/*.sol test/**/*.js utils/**/*.{ts,js} scripts/**/*.{ts,js}",
    "deploy": "hardhat run .\\scripts\\deploy.ts --network local",
    "initialize": "hardhat run .\\scripts\\initialize.ts",
    "compile": "hardhat compile --force",
    "test": "hardhat test --config hardhat.config.test.ts",
    "node": "hardhat node --fork REPLACE_WITH_URL"
  },
  "author": "",
  "license": "BUSL-1.1",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.6",
    "@nomiclabs/hardhat-truffle5": "^2.0.7",
    "decimal.js": "^10.4.3",
    "dotenv-defaults": "^5.0.2",
    "prettier": "^3.2.5",
    "prettier-plugin-solidity": "^1.3.1",
    "solhint": "^4.5.4",
    "solhint-plugin-prettier": "^0.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
